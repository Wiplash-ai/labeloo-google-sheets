{
  "name": "labeloo-google-sheets",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "render:store-assets": "bash scripts/render-store-assets.sh",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm test && npm run build && node scripts/verify.mjs && node scripts/verify-release.mjs",
    "clasp": "clasp"
  },
  "devDependencies": {
    "@google/clasp": "^3.4.0",
    "esbuild": "^0.25.9"
  }
}
