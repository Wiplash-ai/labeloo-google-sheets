{
  "name": "labeloo-google-sheets",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm test && npm run build && node scripts/verify.mjs",
    "clasp": "clasp"
  },
  "devDependencies": {
    "@google/clasp": "^3.4.0",
    "esbuild": "^0.25.9"
  }
}
